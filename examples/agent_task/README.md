# Agent task (Python SDK)

Vision-based agent task end-to-end.

## Prerequisites

- Python 3.10+
- A BrowseFleet server at `http://localhost:3000`
- `ANTHROPIC_API_KEY` or `OPENAI_API_KEY` configured on the server

## Run

```bash
pip install -r requirements.txt
python main.py
```
